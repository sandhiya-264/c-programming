#include <stdio.h>
#include <stdlib.h>
void mul( )
{
    int a,b;
    printf("enter the number");
    scanf("%d %d",&a,&b);

    printf("mul=%d",a*b);
}
int main()
{
    mul();
    return 0;
}
